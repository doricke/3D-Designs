<%= form_for(@comment) do |f| %>
  <% if @comment.errors.any? -%>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| -%>
        <li><%= msg %></li>
      <% end -%>
      </ul>
    </div>
  <% end -%>

  <div class="field">
    <%= f.label :rating %><br>
    <%= f.text_field :rating %>
  </div>
  <div class="field">
   	<!-- %= tinymce_assets % -->
   	<%= tinymce %>
    <%= f.label :comments %><br>
    <%= f.text_area :comments, :rows => 4, :cols => 80, :class => "tinymce" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end -%>
