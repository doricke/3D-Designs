<%= form_for(@used) do |f| %>
  <% if @used.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@used.errors.count, "error") %> prohibited this used from being saved:</h2>

      <ul>
      <% @used.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end -%>
  <div class="field">
    <!-- %= tinymce_assets % -->
    <%= tinymce %>
    <%= f.label :notes %><br>
    <%= f.text_area :notes, :rows => 4, :cols => 80, :class => "tinymce" %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end -%>
