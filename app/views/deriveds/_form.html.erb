<%= form_for(@derived) do |f| %>
  <% if @derived.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@derived.errors.count, "error") %> prohibited this derived from being saved:</h2>

      <ul>
      <% @derived.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :design_id %><br>
    <%= f.number_field :design_id %>
  </div>
  <div class="field">
    <%= f.label :source_id %><br>
    <%= f.number_field :source_id %>
  </div>
  <div class="field">
    <%= f.label :derived_type %><br>
    <%= f.text_field :derived_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
